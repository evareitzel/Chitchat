import { createSlice } from "@reduxjs/toolkit";

const groupsSlice = 
createSlice({
  name: "groups",
  initialState: {
    entities: [],
  },
  reducers: {},
});

export default groupsSlice.reducer;
