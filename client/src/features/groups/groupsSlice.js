import { createSlice } from "@reduxjs/toolkit";

const groupsSlice = 
createSlice({
  name: "groups",
  initialState: {
    entities: [],
  },
  reducers: {
    // add reducers here
  },
});

// export reducers here
export default groupsSlice.reducer;
